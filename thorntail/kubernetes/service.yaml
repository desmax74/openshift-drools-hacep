apiVersion: v1
kind: Service
metadata:
  name: openshift-kie-thorntail
  labels:
    app: openshift-kie-thorntail
spec:
  ports:
    - name: http
      port: 8080
  selector:
    app: openshift-kie-thorntail
