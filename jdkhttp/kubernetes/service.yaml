apiVersion: v1
kind: Service
metadata:
  name: openshift-kie-jdkhttp
  labels:
    app: openshift-kie-jdkhttp
spec:
  ports:
    - name: http
      port: 8080
  selector:
    app: openshift-kie-jdkhttp
